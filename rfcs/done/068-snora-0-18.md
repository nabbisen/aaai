# RFC 068 — snora 0.18.0 dependency update

**Status.** Implemented (v0.28.0)
**Tracks.** Dependency maintenance
**Touches.** `Cargo.toml` (workspace snora version 0.8 → 0.18), `Cargo.lock`.

## Migration analysis

snora follows pre-1.0 semver: minor bumps signal breaking changes.
aaai is upgrading across 10 minor versions (0.8 → 0.18).

| Version | Change | aaai impact |
|---|---|---|
| 0.11.0 | `AppLayout` → `#[non_exhaustive]` | ✅ aaai uses `AppLayout::new()` builder only — no struct literals |
| 0.11.0 | Toast ordering fix (inverted predicate corrected) | ✅ improves `ToastPosition::BottomEnd` display order |
| 0.14.0 | `snora::keyboard::dismiss_on_escape` added | additive |
| 0.17.0 | `Icon` implements `PartialEq` | additive; aaai does not use `Icon` |
| 0.8–0.18 | All other changes: docs, tests, examples, CI | no public API impact |

snora 0.18.0 still targets iced 0.14 — no transitive version conflict.
All 220 tests pass; no warnings.
