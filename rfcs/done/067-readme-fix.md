# RFC 067 — README accuracy fix

**Status.** Implemented (v0.27.0 — Phase 19)
**Touches.** `README.md`.

Command count "15 commands" → "16 commands" to match the actual enum in
`crates/aaai-cli/src/main.rs`.
